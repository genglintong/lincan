/**
 * 题目：学生成绩管理系统（数组优化版）
 * 
 * 要求：
 * 1. 创建一个学生成绩数组，包含至少15个成绩
 * 2. 实现以下功能（每个功能用独立的方法）：
 *    - findMax(int[] scores) : 找最高分
 *    - findMin(int[] scores) : 找最低分  
 *    - calculateAverage(int[] scores) : 计算平均分
 *    - countPass(int[] scores, int passLine) : 统计及格人数
 *    - getTopN(int[] scores, int n) : 获取前N名成绩
 *    - searchScore(int[] scores, int target) : 查找指定分数
 * 3. 在main方法中调用所有方法并输出结果
 * 4. 使用Arrays类的方法进行排序
 * 
 * 进阶要求：
 * - 方法要有返回值，避免直接在方法内打印
 * - 使用增强for循环遍历数组
 * - 计算标准差（可选挑战）
 * 
 * 学习目标：
 * - 方法的设计和重用
 * - Arrays工具类的使用
 * - 代码结构优化
 * - 参数传递和返回值
 */

// 请在下方编写代码